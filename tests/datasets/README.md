# Testdatensätze

Geprüfte Eingaben, Sollverhalten und Herkunft hier ablegen; Entwicklung und unabhängige Abnahme getrennt halten. Noch keine Datensätze vorhanden.

Große lokale Sammlungen liegen unter `local/` (von Git ausgeschlossen) und benötigen eine separate Sicherung. Kleine feste Testfälle können versioniert werden. Siehe den Abschnitt zur Testdaten-Persistenz im Entwicklungsplan.
